eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
  instance:
    hostname: localhost
    
server:
  port: 8081  
  
spring:
  application:
    name: hackathonService  
    
  cloud:
    config:
      uri: http://localhost:8888
    